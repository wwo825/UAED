name: Community

on:
  workflow_dispatch:

jobs:
  detect_pages:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.build_matrix.outputs.matrix }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Detect pages and build matrix
        id: build_matrix
        run: |
          python << 'EOF'
          import json
          import os
          import requests

          URL = "https://wd0ptz13zs-dsn.algolia.net/1/indexes/*/queries"
          PARAMS = {
              "x-algolia-agent": "Algolia for JavaScript (4.24.0); Browser (lite)",
              "x-algolia-api-key": "cdd839b4fdac840289e88633779e8634",
              "x-algolia-application-id": "WD0PTZ13ZS",
          }

          CATEGORIES = {
              #community
              "auto_services": {"index": "by_added_desc_community.com", "filter": '("category_v2.slug_paths":"community/auto-services")'},
              "consultancy_services": {"index": "by_added_desc_community.com", "filter": '("category_v2.slug_paths":"community/consultancy-services")'},
              "domestic": { "index": "by_added_desc_community.com", "filter": '("category_v2.slug_paths":"community/domestic")'},
              "event_entertainment": {"index": "by_added_desc_community.com", "filter": '("category_v2.slug_paths":"community/event-entertainment")'},
              "freelancers": {"index": "by_added_desc_community.com", "filter": '("category_v2.slug_paths":"community/freelancers")'},
              "health_wellbeing_services": {"index": "by_added_desc_community.com", "filter": '("category_v2.slug_paths":"community/health-wellbeing-services")'},
              "home_maintenance": { "index": "by_added_desc_community.com", "filter": '("category_v2.slug_paths":"community/home-maintenance")'},
              "movers_removals": { "index": "by_added_desc_community.com", "filter": '("category_v2.slug_paths":"community/movers-removals")'},
              "other_services": { "index": "by_added_desc_community.com", "filter": '("category_v2.slug_paths":"community/other-services")'},
              "restoration_repairs": { "index": "by_added_desc_community.com", "filter": '("category_v2.slug_paths":"community/restoration-repairs")'},
              "tutors_classes": { "index": "by_added_desc_community.com", "filter": '("category_v2.slug_paths":"community/tutors-classes")'},
              "web_computer_services": { "index": "by_added_desc_community.com", "filter": '("category_v2.slug_paths":"community/web-computer-services")'},
          }

          PAGES_PER_JOB = 30

          matrix = {"include": []}

          for category_name, category in CATEGORIES.items():
              print(f"Detecting pages for {category_name}...")
              try:
                  payload = {
                      "requests": [{
                          "indexName": category["index"],
                          "query": "",
                          "params": f"page=0&hitsPerPage=25&filters={category['filter']}",
                      }]
                  }
                  r = requests.post(URL, params=PARAMS, json=payload, timeout=30)
                  data = r.json()
                  nb_pages = data["results"][0]["nbPages"]

                  print(f"  {category_name}: {nb_pages} pages")
              except Exception as e:
                  print(f"  Failed: {e}")
                  continue

              if nb_pages == 0:
                  continue

              for i in range(0, nb_pages, PAGES_PER_JOB):
                  start = i
                  end = min(i + PAGES_PER_JOB - 1, nb_pages - 1)
                  matrix["include"].append({
                      "category": category_name,
                      "start": start,
                      "end": end
                  })

          print(f"Total jobs: {len(matrix['include'])}")

          with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
              f.write(f"matrix={json.dumps(matrix)}\n")
          EOF

  scrape:
    runs-on: ubuntu-latest
    needs: detect_pages
    strategy:
      matrix: ${{ fromJson(needs.detect_pages.outputs.matrix) }}
      max-parallel: 5
      fail-fast: false

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run scraper with retry
        run: |
          python -u main.py "${{ matrix.category }}" ${{ matrix.start }} ${{ matrix.end }}

      - name: Upload raw data artifact
        uses: actions/upload-artifact@v4
        with:
          name: raw-${{ matrix.category }}-${{ matrix.start }}-${{ matrix.end }}
          path: |
            ${{ matrix.category }}_${{ matrix.start }}_${{ matrix.end }}.jsonl
            ${{ matrix.category }}_${{ matrix.start }}_${{ matrix.end }}_failed.txt
            ${{ matrix.category }}_${{ matrix.start }}_${{ matrix.end }}_result.json
            request_stats_${{ matrix.start }}_${{ matrix.end }}.json
          if-no-files-found: ignore

  merge:
    runs-on: ubuntu-latest
    needs: scrape

    strategy:
      max-parallel: 3
      matrix:
        category:
          - auto_services
          - consultancy_services
          - domestic
          - event_entertainment
          - freelancers
          - health_wellbeing_services
          - home_maintenance
          - movers_removals
          - other_services
          - restoration_repairs
          - tutors_classes
          - web_computer_services

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pandas openpyxl boto3 requests pillow python-dotenv playwright playwright-stealth

      - name: Install Playwright Chrome
        run: playwright install --with-deps chrome

      - name: Download raw data for ${{ matrix.category }}
        uses: actions/download-artifact@v4
        with:
          pattern: raw-${{ matrix.category }}-*
          merge-multiple: true
          path: raw_parts/

      - name: Process and upload to R2
        env:
          CF_R2_ACCESS_KEY_ID: ${{ secrets.CF_R2_ACCESS_KEY_ID }}
          CF_R2_SECRET_ACCESS_KEY: ${{ secrets.CF_R2_SECRET_ACCESS_KEY }}
          CF_R2_ENDPOINT_URL: ${{ secrets.CF_R2_ENDPOINT_URL }}
          CF_R2_BUCKET_NAME: ${{ secrets.CF_R2_BUCKET_NAME }}
          WORKFLOW_NAME: ${{ github.workflow }}
        run: |
          python << 'EOF'
          import glob
          import os
          import json
          import shutil
          import boto3
          import re
          import pandas as pd
          from datetime import datetime
          from dub_writer import process_category

          WORKFLOW_NAME = os.environ.get("WORKFLOW_NAME", "Unknown")

          category = "${{ matrix.category }}"
          jsonl_files = glob.glob('raw_parts/*.jsonl')

          dt = datetime.now()
          date_prefix = f"year={dt.year}/month={dt.strftime('%m')}/day={dt.strftime('%d')}"

          client = boto3.client('s3',
              endpoint_url=os.environ['CF_R2_ENDPOINT_URL'],
              aws_access_key_id=os.environ['CF_R2_ACCESS_KEY_ID'],
              aws_secret_access_key=os.environ['CF_R2_SECRET_ACCESS_KEY'],
              region_name='auto'
          )
          bucket = os.environ['CF_R2_BUCKET_NAME']

          if not jsonl_files:
              print(f"No raw data found for {category}")
              exit(0)

          # Collect failed pages from result files
          failed_pages = []
          total_pages = 0

          for f in glob.glob('raw_parts/*.jsonl'):
              match = re.search(r'_(\d+)_(\d+)\.jsonl$', f)
              if match:
                  start = int(match.group(1))
                  end = int(match.group(2))
                  total_pages += (end - start + 1)

              failed_file = f.replace('.jsonl', '_failed.txt')
              if os.path.exists(failed_file):
                  with open(failed_file, 'r', encoding='utf-8') as ff:
                      for line in ff:
                          if line.startswith('page='):
                              try:
                                  page_num = int(line.split('=')[1])
                                  failed_pages.append(page_num)
                              except:
                                  pass

          failed_percentage = (len(failed_pages) / total_pages * 100) if total_pages > 0 else 0

          # Copy request_stats and failed_pages to output_base so dub_writer can read them
          output_base = "output"
          os.makedirs(output_base, exist_ok=True)
          for stats_f in glob.glob('raw_parts/request_stats_*.json'):
              shutil.copy(stats_f, output_base)

          # Build failed_pages_{category}.json for dub_writer
          failed_pages_data = {
              "category": category,
              "total_failed": len(failed_pages),
              "failed_pages": [
                  {"category": category, "page": p, "error": "Failed after retries"}
                  for p in failed_pages
              ]
          }
          with open(os.path.join(output_base, f"failed_pages_{category}.json"), "w", encoding="utf-8") as f:
              json.dump(failed_pages_data, f, ensure_ascii=False, indent=2)

          # Process data (NO phone_lookup)
          result = process_category(
              category, jsonl_files, output_base,
              upload_images=False, enrich_description=True
          )

          print(f"\nCategory: {category} | Total rows: {result['total']} | Excel files: {len(result['excel_files'])}")
          print(f"Total Pages: {total_pages} | Failed Pages: {len(failed_pages)} | Failed: {failed_percentage:.2f}%")

          # Read category_path from first summary.json found
          category_path = None
          for root, dirs, files in os.walk(output_base):
              for file in files:
                  if file == 'summary.json':
                      try:
                          with open(os.path.join(root, file), 'r', encoding='utf-8') as f:
                              s = json.load(f)
                          category_path = s.get("category_path")
                          if category_path:
                              break
                      except Exception:
                          pass
              if category_path:
                  break

          if not category_path:
              category_path = category.replace("_", "/")
              print(f"Warning: category_path not found in summaries, using fallback: {category_path}")

          print(f"Using category_path: {category_path}")

          # Upload Excel files
          for local_path in result["excel_files"]:
              relative_path = os.path.relpath(local_path, output_base)
              parts = relative_path.split(os.sep)
              # parts = [cat0, cat1, excel, filename.xlsx]
              filename = parts[-1]
              r2_key = f"DUAE/{date_prefix}/{category_path}/excel/{filename}"

              client.upload_file(local_path, bucket, r2_key,
                  ExtraArgs={'ContentType': 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'})
              print(f"Uploaded Excel: {r2_key}")

          # Upload JSON files
          for local_path in result["json_files"]:
              relative_path = os.path.relpath(local_path, output_base)
              parts = relative_path.split(os.sep)
              filename = parts[-1]
              r2_key = f"DUAE/{date_prefix}/{category_path}/json/{filename}"

              client.upload_file(local_path, bucket, r2_key,
                  ExtraArgs={'ContentType': 'application/json; charset=utf-8'})
              print(f"Uploaded JSON: {r2_key}")

          # Save summary placeholder for finalization later
          summary_paths = []
          for root, dirs, files in os.walk('output'):
              for file in files:
                  if file == 'summary.json':
                      summary_paths.append(os.path.join(root, file))

          # Copy summaries to a known location for artifact upload
          os.makedirs("summaries", exist_ok=True)
          for sp in summary_paths:
              try:
                  with open(sp, 'r', encoding='utf-8') as f:
                      s = json.load(f)
                  cp = s.get("category_path", category)
                  # Sanitize for filename
                  safe_cp = cp.replace("/", "_")
                  dest = f"summaries/summary_placeholder_{safe_cp}.json"
              except Exception:
                  dest = f"summaries/summary_placeholder_{category}.json"
              shutil.copy(sp, dest)
              print(f"  Saved summary placeholder: {dest}")

          print(f"\nDONE: {len(result['excel_files'])} excel + {len(result['json_files'])} json files uploaded for {category}")
          EOF

      - name: Upload summary placeholder
        uses: actions/upload-artifact@v4
        with:
          name: summary-${{ matrix.category }}
          path: summaries/*.json
          if-no-files-found: ignore

  finalize-summaries:
    runs-on: ubuntu-latest
    needs: merge
    if: ${{ success() }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install boto3 python-dotenv

      - name: Get workflow duration
        id: workflow_duration
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          STARTED_AT=$(gh api repos/${{ github.repository }}/actions/runs/${{ github.run_id }} --jq .run_started_at)
          START_TIMESTAMP=$(date -d "$STARTED_AT" +%s)
          NOW_TIMESTAMP=$(date -u +%s)
          DURATION_SEC=$((NOW_TIMESTAMP - START_TIMESTAMP))
          echo "duration_sec=$DURATION_SEC" >> "$GITHUB_OUTPUT"
          echo "Workflow started at: $STARTED_AT"
          echo "Workflow duration: ${DURATION_SEC}s"

      - name: Download all summary placeholders
        uses: actions/download-artifact@v4
        with:
          pattern: summary-*
          merge-multiple: true
          path: summaries/

      - name: Finalize and upload summaries to R2
        env:
          CF_R2_ACCESS_KEY_ID: ${{ secrets.CF_R2_ACCESS_KEY_ID }}
          CF_R2_SECRET_ACCESS_KEY: ${{ secrets.CF_R2_SECRET_ACCESS_KEY }}
          CF_R2_ENDPOINT_URL: ${{ secrets.CF_R2_ENDPOINT_URL }}
          CF_R2_BUCKET_NAME: ${{ secrets.CF_R2_BUCKET_NAME }}
          WORKFLOW_DURATION: ${{ steps.workflow_duration.outputs.duration_sec }}
        run: |
          python finalize_summaries.py --summaries-dir summaries/ --workflow "${{ github.workflow }}"